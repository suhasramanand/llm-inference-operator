---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: llminferenceclusters.inference.llm.io
spec:
  group: inference.llm.io
  names:
    kind: LLMInferenceCluster
    listKind: LLMInferenceClusterList
    plural: llminferenceclusters
    singular: llminferencecluster
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LLMInferenceCluster is the Schema for the llminferenceclusters
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of LLMInferenceCluster
            properties:
              autoscaling:
                description: autoscaling configures token/queue/pressure-driven scaling.
                properties:
                  enabled:
                    description: enabled controls whether the operator should actively
                      scale workloads.
                    type: boolean
                  maxReplicas:
                    description: maxReplicas bounds decode scaling.
                    format: int32
                    minimum: 1
                    type: integer
                  minReplicas:
                    description: minReplicas bounds decode scaling.
                    format: int32
                    minimum: 0
                    type: integer
                  signals:
                    description: signals configures the metrics/signal source.
                    properties:
                      prometheusURL:
                        description: prometheusURL is used when source=prometheus.
                        type: string
                      source:
                        description: source selects which adapter powers autoscaling
                          decisions.
                        enum:
                        - fake
                        - prometheus
                        type: string
                    type: object
                type: object
              kvCache:
                description: kvCache configures KV cache strategy and shard tracking.
                properties:
                  evictionPolicy:
                    description: evictionPolicy is the eviction strategy used when
                      under memory pressure.
                    enum:
                    - lru
                    - ttl
                    - attentionAwareStub
                    type: string
                  numShards:
                    description: numShards is the number of KV shards to publish/track.
                    format: int32
                    minimum: 1
                    type: integer
                  strategy:
                    description: strategy describes how the cache is distributed.
                    enum:
                    - sharded
                    type: string
                  warmCache:
                    description: warmCache configures a second-tier cache (local-only
                      in v1).
                    enum:
                    - none
                    - memory
                    - diskStub
                    type: string
                type: object
              model:
                description: model defines the worker image and configuration.
                properties:
                  args:
                    description: args are passed to the worker container entrypoint.
                    items:
                      type: string
                    type: array
                  env:
                    description: env are additional environment variables for the
                      worker container.
                    items:
                      properties:
                        name:
                          minLength: 1
                          type: string
                        value:
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                  image:
                    description: |-
                      image is the container image for the inference worker.
                      In local v1, this may point to a mock worker.
                    minLength: 1
                    type: string
                  name:
                    description: name is a human-friendly model identifier (e.g. "llama3-8b").
                    minLength: 1
                    type: string
                required:
                - image
                - name
                type: object
              routing:
                description: routing configures the session-aware router.
                properties:
                  hashKey:
                    description: hashKey controls what request field is used to provide
                      stable routing.
                    minLength: 1
                    type: string
                  mode:
                    description: 'mode controls routing semantics (v1: session affinity).'
                    enum:
                    - sessionAffinity
                    type: string
                  routerReplicas:
                    description: routerReplicas is the desired replica count for the
                      router service.
                    format: int32
                    minimum: 1
                    type: integer
                type: object
              runtime:
                description: runtime defines prefill vs decode role sizing.
                properties:
                  decode:
                    description: decode is the (latency-sensitive) decode workload.
                    properties:
                      podLabels:
                        additionalProperties:
                          type: string
                        description: podLabels are merged into the pod template labels.
                        type: object
                      replicas:
                        description: replicas is the desired replica count for this
                          role.
                        format: int32
                        minimum: 0
                        type: integer
                      servicePort:
                        description: servicePort is the HTTP port exposed by the role
                          Service.
                        format: int32
                        maximum: 65535
                        minimum: 1
                        type: integer
                    type: object
                  prefill:
                    description: prefill is the (typically heavier) prefill workload.
                    properties:
                      podLabels:
                        additionalProperties:
                          type: string
                        description: podLabels are merged into the pod template labels.
                        type: object
                      replicas:
                        description: replicas is the desired replica count for this
                          role.
                        format: int32
                        minimum: 0
                        type: integer
                      servicePort:
                        description: servicePort is the HTTP port exposed by the role
                          Service.
                        format: int32
                        maximum: 65535
                        minimum: 1
                        type: integer
                    type: object
                type: object
            required:
            - model
            type: object
          status:
            description: status defines the observed state of LLMInferenceCluster
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the LLMInferenceCluster resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              endpoints:
                description: endpoints summarizes the primary access URLs for the
                  cluster.
                properties:
                  decodeService:
                    description: decodeService is the in-cluster Service for decode.
                    type: string
                  prefillService:
                    description: prefillService is the in-cluster Service for prefill.
                    type: string
                  routerURL:
                    description: routerURL is the primary endpoint for clients.
                    type: string
                type: object
              observedGeneration:
                description: observedGeneration is the last generation acted on by
                  the controller.
                format: int64
                type: integer
              shards:
                description: shards is the published shard ownership map.
                items:
                  properties:
                    lastHeartbeat:
                      description: lastHeartbeat is updated when the owning pod reports
                        health.
                      format: date-time
                      type: string
                    ownerPod:
                      description: ownerPod is the pod name that currently owns the
                        shard.
                      type: string
                    phase:
                      description: phase is the current shard state.
                      enum:
                      - Pending
                      - Ready
                      - Degraded
                      type: string
                    role:
                      description: role is the workload role owning the shard (prefill/decode).
                      enum:
                      - prefill
                      - decode
                      type: string
                    shardID:
                      description: shardID is the numeric shard identifier.
                      format: int32
                      minimum: 0
                      type: integer
                  required:
                  - role
                  - shardID
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
